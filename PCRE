/**
 *  PCRE PATTERNS
 *
 *   .          любой символ
 *   .*         любое колличество символов 
 *   ?          ноль или один символ {0,1}
 *   *          ноль или любое колличество символов {0,}
 *   +          один или любое колличество символов {1,}
 *   a          все слова где есть буква a
 *   ^          начало строки (вне символьного класса)
 *   ^          отрицание (внутри символьного класса)
 *   $          конец строки
 *   |          опператор or
 *   -          опператор range
 *  []          символьный класс
 *  [0-9]       число в диаппазоне
 *  ()          группа для обьединения выражения
 *  (x{4,5})    колличество символов x в диапозоне
 *
 *   КОНСТАНТЫ
 *   i          регистронезависимое
 *
 *   МОДИФИКАТОРЫ
 *   \s        пустая строка
 * 
 * 
 *   ПРИМЕР
 *   $result = preg_grep('/^c.*c$/',['cdfgdgfc']);  //cdfgdgfc
 */
